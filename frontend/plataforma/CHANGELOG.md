# Changelog

## [1.0.0] - Initial Refactor

### Fixed
- TypeScript errors in API responses
- User model inconsistencies between store and service
- Import path issues in favorites store
- Duplicate route registration in router

### Added
- robots.txt for SEO
- Accessibility improvements (A11Y WCAG AA)
- Semantic HTML structure with `<main>` landmarks
- ARIA labels for interactive elements
- Alt text for images

### Improved
- DOM structure optimization
- CSS cleanup and deduplication
- Button accessibility with proper labels
- Heading hierarchy (h1 → h2 → h3)